describe('Register Action', () => {
	describe('Valid', () => {
		it.todo('Should register a new user');

		it.todo('Should set cookies when registering a new user');
	});

	describe('Invalid', () => {
		it.todo('Should return error response when username is taken');

		it.todo(
			'Should return error response when username is less than 3 characters',
		);

		it.todo(
			'Should return error response when username is more than 50 characters',
		);

		it.todo(
			'Should return error response when password is less than 8 characters',
		);
	});
});
